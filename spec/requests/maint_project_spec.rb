
describe "Existing users can create new projects under accounts where they are account admin" do
  it "should flash success given good attributes" do
    pending
  end
end

describe "Existing users can suspend project under accounts where they are account admin" do
  it "should show suspend command if user is account admin" do
    pending
  end
  it "should show reinstante command and suspended state if user is account admin" do
    pending
  end
end

describe "Existing users can reinstate suspended projects under accounts where they are account admin" do
  it "should show suspend command and something" do
    pending
  end
end

describe "Existing users can not suspend projects under accounts where they are not account admin" do
  it "should not show suspend command" do
    pending
  end
end

describe "Existing users can not navigate to suspended projects" do
  it "should show the suspended project, but not link to project home page" do
    pending
  end
end
