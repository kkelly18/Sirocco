<% if @sponsorships.any? %>
  <table class="index_grid">
    <%= render :partial => 'sponsorships/sponsorship', :collection => @sponsorships %>
  </table>
  <%= will_paginate @sponsorships %>
<% end %>