<li>
  <%= gravatar_for team_member, :size => 30 %>
  <%= link_to team_member.name, team_member %>
  <% if current_user.sponsorships.where(:account_id=>@account.id).where(:admin=>true).exists? %>
  |
  <%= link_to "suspend", '#', :method => :delete, :confirm => "You sure?", :title => "Suspend #{team_member.name}" %>

  <% end %>
</li>